# 変更提案: XDG準拠の保存パス解決

## 背景
認証・予算トラッカーの保存先が `$HOME/.config/xcom-rs` に固定されており、XDG環境変数を尊重していません。

## 目的
- `XDG_CONFIG_HOME` / `XDG_DATA_HOME` を尊重した保存先解決にする
- CIや隔離テストで意図せぬ実ファイル汚染を防ぐ

## スコープ
- 認証ストレージと予算トラッカーのデフォルトパス解決

## 非スコープ
- 既存データの自動移行

## 成功条件
- XDG変数が設定されている場合、該当パスが優先される
- 変数未設定時は従来パスを維持する
