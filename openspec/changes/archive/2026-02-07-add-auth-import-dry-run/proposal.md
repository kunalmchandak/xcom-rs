# 変更提案: auth import のdry-run/計画出力

## 背景
`auth import` は即書き込みで、衝突や更新内容の事前把握ができません。

## 目的
- 実書き込み前に変更内容を確認できるようにする
- 機械可読な計画出力で自動化に適合させる

## スコープ
- `auth import` に `--dry-run` を追加
- 変更計画の要約と詳細を出力

## 非スコープ
- 実際の暗号化/復号方式の変更

## 成功条件
- `--dry-run` は保存ファイルを変更しない
- 変更計画が `text/json` で取得できる
